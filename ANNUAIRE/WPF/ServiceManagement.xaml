<Window x:Class="WPF.ServiceManagement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestion des Services" Height="450" Width="800">

    <Grid Margin="10">
        <StackPanel Orientation="Vertical">

            <!-- Tableau des services -->
            <DataGrid x:Name="ServiceDataGrid" AutoGenerateColumns="False" ItemsSource="{Binding Services}"
                      CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single" Margin="0,0,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding IdService}" Width="100"/>
                    <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="*"/>

                    <!-- Colonne Actions -->
                    <DataGridTemplateColumn Header="Actions" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Modifier" Click="EditService_Click" Tag="{Binding}"
                                            Width="75" Margin="5"/>
                                    <Button Content="Supprimer" Click="DeleteService_Click" Tag="{Binding IdService}"
                                            Background="#9981c8" Foreground="White" Width="75"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Boutons en bas -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Ajouter un Service" Click="AddService_Click" Width="150"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
